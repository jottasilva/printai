export const metadata = {
  title: 'Painel Admin | PrintAI ERP',
  description: 'Dashboard administrativo do PrintAI ERP',
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
